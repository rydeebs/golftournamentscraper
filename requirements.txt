# Use streamlit version with pre-built wheels
streamlit>=1.24.0,<1.28.0
requests>=2.28.0
beautifulsoup4>=4.11.0
# Use an older pandas version with better compatibility
pandas>=1.5.0,<2.0.0
python-dateutil>=2.8.0
# Keep lxml as optional, commented out by default
# lxml>=4.9.0
# Optional: For JavaScript-rendered pages
# selenium>=4.10.0
# webdriver-manager>=3.8.0

# Also install these dependencies to help resolve installation issues
# Pre-built wheels should be available for most environments
numpy>=1.20.0
Pillow>=9.0.0,<9.5.0
